/*
 * This is Comments
 */

// 'Preprocessor Directives' starts from '#'
// Imports Libraries
#include <stdio.h>

// Identifiers : a, b, main, ...
int main(void)
// Punctuations : curly braces ({}) and semi-colons (;)
{
	int a = 0;
	// Operators : +. &, etc...
	scanf("%d", &a);
	printf("%d", a);
}
